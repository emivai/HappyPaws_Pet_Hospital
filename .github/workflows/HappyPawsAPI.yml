# name: Build and deploy .NET Core application to Web App HappyPawsAPI with API Management Service HappyPawsAPIapi-HappyPawsAPI
# on:
#   push:
#     branches:
#     - authorization
# env:
#   AZURE_WEBAPP_NAME: HappyPawsAPI
#   AZURE_WEBAPP_PACKAGE_PATH: .\publish
#   AZURE_APIM_RESOURCE_PATH: /happypaws
#   AZURE_APIM_RESOURCEGROUP: HappyPawsAPI20231119190646ResourceGroup
#   AZURE_APIM_SERVICENAME: HappyPawsAPIapi
#   AZURE_APIM_API_ID: HappyPawsAPI
#   AZURE_APIM_APPSERVICEURL: https://happypawsapi.azurewebsites.net
#   SWASHBUCLE_ASPNET_CORE_CLI_PACKAGE_VERSION: 5.6.3
#   SWASHBUCKLE_DOTNET_CORE_VERSION: 3.1.x
#   API_IMPORT_SPECIFICATION_PATH: .\publish\swagger.json
#   API_IMPORT_DLL: .\bin\Release\net6.0\HappyPaws.API.dll
#   API_IMPORT_VERSION: v1
#   CONFIGURATION: Release
#   DOTNET_CORE_VERSION: 6.0.x
#   WORKING_DIRECTORY: .
# jobs:
#   build:
#     runs-on: windows-latest
#     steps:
#     - uses: actions/checkout@v3
#     - name: Setup .NET SDK
#       uses: actions/setup-dotnet@v3
#       with:
#         dotnet-version: ${{ env.DOTNET_CORE_VERSION }}
#     - name: Setup SwashBuckle .NET Core
#       uses: actions/setup-dotnet@v3
#       with:
#         dotnet-version: ${{ env.SWASHBUCKLE_DOTNET_CORE_VERSION }}
#     - name: Restore
#       run: dotnet restore ${{ env.WORKING_DIRECTORY }}
#     - name: Build
#       run: dotnet build ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-restore
#     - name: Test
#       run: dotnet test ${{ env.WORKING_DIRECTORY }} --no-build
#     - name: Publish
#       run: dotnet publish ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-build --output ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
#       working-directory: ${{ env.WORKING_DIRECTORY }}
#     - name: Generate Open API Specification Document
#       run: swagger tofile --output "${{ env.API_IMPORT_SPECIFICATION_PATH }}" "${{ env.API_IMPORT_DLL }}" "${{ env.API_IMPORT_VERSION }}"
#     - name: Publish Artifacts
#       uses: actions/upload-artifact@v3
#       with:
#         name: webapp
#         path: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
#   deploy:
#     runs-on: windows-latest
#     needs: build
#     steps:
#     - name: Download artifact from build job
#       uses: actions/download-artifact@v3
#       with:
#         name: webapp
#         path: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
#     - name: Deploy to Azure WebApp
#       uses: azure/webapps-deploy@v2
#       with:
#         app-name: ${{ env.AZURE_WEBAPP_NAME }}
#         package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
#         publish-profile: ${{ secrets.HappyPawsAPI_979A }}


name: Build and deploy ASP.Net Core app to Azure Web App - happypaws

on:
  push:
    branches:
      - authorization
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'
          include-prerelease: true

      - name: Build with dotnet
        run: dotnet build --configuration Release

      - name: dotnet publish
        run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapp

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v2
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v2
        with:
          name: .net-app

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'happypawsapi'
          slot-name: 'Production'
          publish-profile: ${{ secrets.HappyPawsAPI_979A  }}
          package: .
